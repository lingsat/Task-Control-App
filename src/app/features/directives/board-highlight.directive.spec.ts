import { BoardHighlightDirective } from './board-highlight.directive';

describe('BoardHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BoardHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
